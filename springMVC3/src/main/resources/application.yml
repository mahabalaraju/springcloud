server:
  port: 8099
spring:
  datasource:
    url: jdbc:mysql://mysqldb:3306/spring
    username: admin
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format-sql: true
    show-sql: true
